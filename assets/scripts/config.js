'use strict'

// ADD MODULE AND BUTTONS FOR AJAX THEN GO BACK INTO BACKEND AND USE DB TO CHECK

const config = {
  apiOrigins: {
    development: 'http://localhost:4741',
    production: 'https://ga-wdi-boston.herokuapp.com'
  }
}

module.exports = config
